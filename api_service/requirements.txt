# 论文格式检测系统依赖包

# FastAPI核心框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 数据验证
pydantic==2.5.0

# Word文档处理
python-docx==1.1.0

# 批注支持（python-docx扩展）
# 注意：批注功能可能需要特定版本或扩展库
# 如果项目使用了自定义的批注实现，可能不需要额外的库

# 可选：如果使用了其他功能
# requests==2.31.0  # 用于HTTP请求
# aiofiles==23.2.1  # 异步文件操作
